import { styled } from "../../../../stitches.cofing";

export const Container = styled("div", {
    display: "flex",
    alignItems: "center",
    gap: "$3",
  
    svg: {
      color: "$green100"
    }
  })